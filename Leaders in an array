int [] arr= {1,2,3,4,5};
		Stack<Integer> stack=new Stack<>();
		int max=0;
		for(int i=arr.length-1;i>=0;i--)
		{
			if(arr[i]>max)
			{
				stack.push(arr[i]);
				max=arr[i];
			}
			
		}
		System.out.println(stack);
